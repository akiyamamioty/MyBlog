from django.conf.urls import *
from blog.views import archive, search, single 

 
urlpatterns = patterns('',
                      #url(r'^$',archive),
                      #url(r'^search/$',search),
                      #url(r'^single.html/$',single),
                      )